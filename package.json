{
  "name": "teardrop-token-visualizer",
  "version": "1.0.0",
  "description": "Standalone interactive visualizer for design tokens with relationship graph. Supports JSON file upload.",
  "private": false,
  "scripts": {
    "build-graph": "node scripts/build-graph.js",
    "dev": "npm run build-graph && npx vite",
    "build": "npm run build-graph && npx vite build",
    "preview": "npx vite preview"
  },
  "dependencies": {
    "@radix-ui/react-dropdown-menu": "^2.1.16",
    "@radix-ui/react-icons": "^1.3.2",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-force-graph-2d": "^1.25.6"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^5.1.2",
    "vite": "^6.4.1"
  }
}
